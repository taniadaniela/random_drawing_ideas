import React from 'react';
import { render } from 'react-dom';
import ScheduleForm from './ScheduleForm';

const Application = () => (
  <div>
    <p>test</p>
  </div>
  
);

render(<ScheduleForm />, document.getElementById('root'));